!!!
%html
  %head
    %title Koerahoidja
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    / %nav.gray.header
    /   .navbar-brand.blue_text
    /     KOERAHOIDJA
    /   .navbar-text
    /     - if @current_user
    /       = @current_user.name
    /   %ul.nav.navbar-nav.navbar-right
    /     %li.navbar-text
    /       - if @current_user
    /         .glyphicon.glyphicon-cog
    /       - else
    /         .glyphicon.glyphicon-plus
    /     %li.navbar-text
    /       holllla



    / %nav.navbar.navbar-default{:role => "navigation"}
    /   .navbar-header
    /     %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
    /       %span.sr-only Toggle navigation
    /       %span.icon-bar
    /       %span.icon-bar
    /       %span.icon-bar
    /     %a.navbar-brand{:href => "#"}= t('koerahoidja')

    /   #bs-example-navbar-collapse-1.collapse.navbar-collapse
    /     %ul.nav.navbar-nav.navbar-right
    /       %li.navbar-text
    /         - if @current_user
    /           = @current_user.email
    /       %li
    /         = link_to '#' do
    /           - if @current_user
    /             = t('settings')
    /           - else
    /             = t('add-profile')






    %nav.navbar.navbar-fixed-top.navbar-default{:role => "navigation"}
      .navbar-header
        =link_to t(:koerahoidja), nil, {class: 'navbar-brand uppercase letter-spacing '}
      %ul.nav.navbar-nav.navbar-collapse.navbar-right
        %li.navbar-text
          - if @current_user
            = @current_user.email
        %li.dropdown
          / =link_to t(:settings), '#', {class: 'dropdown-toggle'} do
          %ul.dropdown-menu
            %li
              Profiil
            %li
              Andmed
            %li
              Kirjeldus
            %li.divider
            %li
              Logi välja

    .container
      .row
        .col-md-12
          = flash[:alert]
          = flash[:notice]

      = yield

    .row.center
      %hr
      SO COPYRIGHT WOW
      %hr